{
  "name": "ajglobe",
  "version": "0.0.1",
  "description": "General, fast orthographic globe rendering of points, lines, and polygons — geometry lives on the GPU (60 FPS at 1M+ DGGS cells), antimeridian/pole-correct by construction.",
  "type": "module",
  "main": "./dist/ajglobe.min.js",
  "module": "./dist/ajglobe.min.js",
  "unpkg": "./dist/ajglobe.iife.min.js",
  "jsdelivr": "./dist/ajglobe.iife.min.js",
  "exports": {
    ".": "./dist/ajglobe.min.js",
    "./iife": "./dist/ajglobe.iife.min.js",
    "./src/orb.js": "./src/orb.js"
  },
  "sideEffects": false,
  "files": [
    "dist",
    "src",
    "README.md",
    "LICENSE"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ajfriend/ajglobe.git"
  },
  "homepage": "https://github.com/ajfriend/ajglobe#readme",
  "bugs": "https://github.com/ajfriend/ajglobe/issues",
  "author": "AJ Friend",
  "scripts": {
    "serve": "uv run -m http.server 8080",
    "test": "node --test",
    "build": "npm run build:esm && npm run build:iife",
    "build:esm": "esbuild src/orb.js --bundle --format=esm --minify --sourcemap --external:https://* --outfile=dist/ajglobe.min.js",
    "build:iife": "esbuild src/orb.js --bundle --format=iife --global-name=ajglobe --minify --sourcemap --external:https://* --outfile=dist/ajglobe.iife.min.js",
    "prepack": "npm run build"
  },
  "keywords": [
    "globe",
    "orthographic",
    "webgl2",
    "geojson",
    "map",
    "dggs",
    "visualization"
  ],
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
